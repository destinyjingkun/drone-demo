kind: pipeline
type: docker
name: backend

steps:
- name: build
  image: golang:1.12
  commands:
  - go build
  when:
    branch: [master]

---
kind: pipeline
type: exec
name: frontend

steps:
- name: build
  image: golang:1.12
  commands:
  - echo exec here
  when:
    branch: [master]
